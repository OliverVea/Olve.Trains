[project]
name = "olve-trains-integration"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0",
    "pillow>=10.0",
    "numpy>=1.26",
]

[tool.pytest.ini_options]
testpaths = ["scenarios"]
